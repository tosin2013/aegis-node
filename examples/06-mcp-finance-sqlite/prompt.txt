Use the sqlite__list_tables tool to see what tables exist, then sqlite__describe_table on the expenses table to see its schema. Then run sqlite__read_query with "SELECT * FROM expenses WHERE amount_usd > 1000 ORDER BY amount_usd DESC" to find expenses over $1000. Write a one-paragraph anomaly report to /tmp/aegis-example-06/output/q2-expense-anomalies.md using filesystem__write. List each flagged expense with department, vendor, and amount, and call out anything unusual (vendors that appear once, amounts way above category norms). Then stop.
